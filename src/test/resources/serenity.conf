webdriver {
  driver = chrome
}

chrome {
  switches = "--start-maximized"
}

environments {
  ci {
    chrome.switches = "--headless=new", "--no-sandbox", "--disable-dev-shm-usage", "--disable-gpu", "--window-size=1920,1080"
  }
}

serenity.project.name = "Mercado Libre Automation (Java 17)"
